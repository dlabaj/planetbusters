apply plugin: "java"

sourceSets.main.java.srcDirs = [ "src/" ]

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    // Build with JDK 21 but target Java 8 bytecode for compatibility with LibGDX 1.7.x
    options.release = 8
}
